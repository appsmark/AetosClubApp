name: aetos
description: Aetos Team App

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 3.1.0+310000

environment:
  sdk: '>=2.19.4 <=3.4.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.1
  intl: ^0.18.1
  material: ^1.0.0+2
  rss_dart: ^1.0.6
  shared_preferences: ^2.2.0
  provider: ^6.1.1
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/splashscreen.png
